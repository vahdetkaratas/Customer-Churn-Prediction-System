# Static (FastAPI-served)

| File | Purpose |
|------|---------|
| `index.html` | Landing + predict form (`GET /`) — visual style aligned with Batch Scoring `vercel_static` (Outfit, amber accent, dark + grain) |

Charts and other root URLs live in **`../public/`** (Vercel CDN + `GET /model_comparison.png` in `app.py` for local dev).
