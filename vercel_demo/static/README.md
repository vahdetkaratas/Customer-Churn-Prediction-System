# Static (FastAPI-served)

| File | Purpose |
|------|---------|
| `index.html` | Landing + predict form (`GET /`) — **Arter / vahdetkaratas.com** shell (Poppins, `#FFC107`, 1440px card, info bar + rail). Prompt pack: [`docs/PORTFOLIO_DEMO_THEME_PROMPT.md`](../../docs/PORTFOLIO_DEMO_THEME_PROMPT.md) |

Charts and other root URLs live in **`../public/`** (Vercel CDN + `GET /model_comparison.png` in `app.py` for local dev).
